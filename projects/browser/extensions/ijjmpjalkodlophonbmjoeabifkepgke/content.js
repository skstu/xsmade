(function () {
  window.addEventListener("load", () => {
    // const curUrl = window.location.href;
    // if (
    //   typeof chrome !== "undefined" &&
    //   chrome.runtime &&
    //   chrome.runtime.sendMessage
    // ) {
    //   chrome.runtime.sendMessage(
    //     { type: "content", url: curUrl },
    //     (response) => {
    //       console.log("✅ JSON 已发送到后台：", response);
    //     }
    //   );
    // } else {
    //   console.log("chrome sendMessage 不可用");
    // }
  });
})();
